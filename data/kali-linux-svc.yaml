apiVersion: v1
kind: Service
metadata:
  name: kalilinux
  labels:
    app: kalilinux
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 22
      targetPort: 22
  selector:
    app: kalilinux
